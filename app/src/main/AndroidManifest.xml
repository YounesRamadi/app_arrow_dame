<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apadnom">

    <uses-feature android:name="android.hardware.bluetooth" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Shooting Stars"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apadnom">
        <activity android:name="activities.HistoriqueActivity"/>
        <activity android:name="activities.BluetoothActivity" />
        <activity android:name="activities.ChooseGameActivity" />
        <activity android:name="activities.DisplayBoardActivity" />
        <activity android:name="activities.SettingsActivity" />
        <activity android:name="activities.DisplayBoardIaActivity" />
        <activity android:name="activities.LoginActivity" />
        <activity android:name="activities.MenuActivity" />
        <activity android:name="activities.TutorielActivity" />
        <activity android:name="activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>